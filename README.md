# webp-sh

Shell Scripts using webp

**Important**

Install `webp` first using homebrew or npm's `cwebp`.

## Installation by `homebrew`

```sh
brew install webp
```

## Installation by `npm`

```sh
npm i cwebp
```
